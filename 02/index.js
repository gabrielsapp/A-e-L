const letras = ["A", "a", "B", "C", "E", "e"];

let contagem = 0;
for(i of letras){
    if(i === "E" || i === "e"){
        contagem++
        
        
    }

}

console.log(`Foram encontradas ${contagem} letra "E" ou "e"`)

